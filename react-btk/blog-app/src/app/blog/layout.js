export default function BlogLayout({ children }) {
    return (
        <html lang="en">
            <body>
                <h1>Blog Layout</h1>
                {children}
            </body>
        </html>
    );
}